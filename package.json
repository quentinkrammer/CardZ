{
  "name": "cards",
  "version": "0.0.0",
  "devDependencies": {
    "prettier": "^3.4.2"
  },
  "type": "module",
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "stylecheck": "prettier . --check --config ./.prettierrc.cjs",
    "stylefix": "prettier . --write --config ./.prettierrc.cjs"
  }
}
